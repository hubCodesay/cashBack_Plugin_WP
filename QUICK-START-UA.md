# Швидкий старт - WooCommerce Cashback System

## Крок 1: Перевірка WooCommerce

**Перед активацією плагіна переконайтеся, що WooCommerce встановлено та активовано!**

1. Перейдіть: **Плагіни → Встановлені плагіни**
2. Знайдіть **WooCommerce**
3. Якщо він **не активний** - натисніть **Активувати**
4. Якщо його **немає** - встановіть його з каталогу плагінів WordPress

## Крок 2: Активація Cashback System

1. Перейдіть: **Плагіни → Встановлені плагіни**
2. Знайдіть **WooCommerce Cashback System**
3. Натисніть **Активувати**

## Крок 3: Доступ до налаштувань

Після активації у вас з'явиться нове меню в адмін панелі:

### Розташування меню:
```
WordPress Admin Dashboard
├── Панель управління
├── ...
├── WooCommerce
├── 💰 Cashback  ← НОВЕ МЕНЮ
│   ├── Settings (Налаштування)
│   ├── Manage Users (Управління користувачами)
│   └── Statistics (Статистика)
└── ...
```

### Іконка меню:
Шукайте іконку **💰 зеленого долара** в лівому меню адміністратора.

## Якщо не бачите меню "Cashback":

### Перевірка 1: WooCommerce активний?
```
Плагіни → Встановлені плагіни → Перевірте статус WooCommerce
```

### Перевірка 2: Cashback System активний?
```
Плагіни → Встановлені плагіни → Перевірте статус WooCommerce Cashback System
```

### Перевірка 3: Права доступу
Переконайтеся, що ви увійшли як **Адміністратор**.
Меню "Cashback" доступне лише користувачам з правами **manage_woocommerce**.

### Перевірка 4: Помилки активації
1. Перейдіть: **Плагіни → Встановлені плагіни**
2. Деактивуйте плагін
3. Знову активуйте плагін
4. Перевірте наявність помилок у верхній частині екрану

## Прямий доступ до сторінок

Якщо меню не відображається, спробуйте прямі посилання:

### Налаштування:
```
ВАШ_САЙТ/wp-admin/admin.php?page=wcs-cashback
```

### Управління користувачами:
```
ВАШ_САЙТ/wp-admin/admin.php?page=wcs-cashback-users
```

### Статистика:
```
ВАШ_САЙТ/wp-admin/admin.php?page=wcs-cashback-stats
```

Замініть `ВАШ_САЙТ` на вашу адресу (наприклад, `http://localhost/plugin-sale`)

## Перевірка встановлення через PHP

Створіть файл `test-cashback.php` в корені WordPress:

```php
<?php
// test-cashback.php
require_once('wp-load.php');

echo "<h1>Перевірка WooCommerce Cashback System</h1>";

// Перевірка WooCommerce
if (class_exists('WooCommerce')) {
    echo "✅ WooCommerce встановлено і активно<br>";
} else {
    echo "❌ WooCommerce НЕ активний!<br>";
}

// Перевірка плагіна
if (class_exists('WCS_Cashback_System')) {
    echo "✅ Cashback System встановлено і активно<br>";
} else {
    echo "❌ Cashback System НЕ активний!<br>";
}

// Перевірка таблиць бази даних
global $wpdb;
$table_balances = $wpdb->prefix . 'wcs_cashback_balances';
$table_transactions = $wpdb->prefix . 'wcs_cashback_transactions';

$balances_exists = $wpdb->get_var("SHOW TABLES LIKE '$table_balances'");
$transactions_exists = $wpdb->get_var("SHOW TABLES LIKE '$table_transactions'");

if ($balances_exists) {
    echo "✅ Таблиця балансів створена<br>";
} else {
    echo "❌ Таблиця балансів НЕ створена!<br>";
}

if ($transactions_exists) {
    echo "✅ Таблиця транзакцій створена<br>";
} else {
    echo "❌ Таблиця транзакцій НЕ створена!<br>";
}

// Перевірка налаштувань
$settings = get_option('wcs_cashback_settings');
if ($settings) {
    echo "✅ Налаштування збережені<br>";
    echo "<pre>";
    print_r($settings);
    echo "</pre>";
} else {
    echo "❌ Налаштування НЕ знайдені!<br>";
}
?>
```

Відкрийте в браузері: `http://ВАШ_САЙТ/test-cashback.php`

## Ручна активація через MySQL

Якщо плагін не активується автоматично:

```sql
-- Вставити налаштування за замовчуванням
INSERT INTO wp_options (option_name, option_value, autoload) 
VALUES ('wcs_cashback_settings', 'a:9:{s:7:"enabled";s:3:"yes";s:16:"tier_1_threshold";d:500;s:17:"tier_1_percentage";d:3;s:16:"tier_2_threshold";d:1000;s:17:"tier_2_percentage";d:5;s:16:"tier_3_threshold";d:1500;s:17:"tier_3_percentage";d:7;s:18:"max_cashback_limit";d:10000;s:24:"usage_limit_percentage";d:50;s:20:"enable_notifications";s:3:"yes";}', 'yes');
```

## Типові проблеми та рішення

### Проблема: "Cashback System requires WooCommerce"
**Рішення:** Спочатку встановіть та активуйте WooCommerce

### Проблема: Меню не відображається
**Рішення:** 
1. Перевірте права користувача (має бути Administrator)
2. Очистіть кеш браузера (Ctrl+Shift+Delete)
3. Деактивуйте/активуйте плагін знову

### Проблема: Помилка бази даних
**Рішення:**
1. Перейдіть: **Інструменти → Здоров'я сайту**
2. Перевірте з'єднання з базою даних
3. Перевірте права доступу до БД

### Проблема: Білий екран після активації
**Рішення:**
1. Перевірте логи помилок PHP
2. Увімкніть WP_DEBUG в wp-config.php:
```php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
```
3. Перегляньте файл: wp-content/debug.log

## Потрібна допомога?

### Перевірте:
1. Версію PHP (потрібна 7.4+)
2. Версію WordPress (потрібна 5.8+)
3. Версію WooCommerce (потрібна 5.0+)
4. Права на запис у папки плагіна

### Логи для перевірки:
- `wp-content/debug.log` - помилки WordPress
- Консоль браузера (F12) - помилки JavaScript
- Логи сервера - помилки PHP

## Скріншоти розташування меню

Після активації ви побачите:

```
┌─────────────────────────────────┐
│ WordPress Admin Menu            │
├─────────────────────────────────┤
│ 📊 Панель управління           │
│ 📝 Записи                       │
│ 📄 Сторінки                     │
│ 💬 Коментарі                    │
│ 🎨 Зовнішній вигляд            │
│ 🔌 Плагіни                      │
│ 👥 Користувачі                  │
│ 🛠️ Інструменти                  │
│ ⚙️ Налаштування                 │
│ 🛒 WooCommerce                  │
│ 💰 Cashback ← ЗНАЙДІТЬ ТУТ!   │
│   ├── Settings                  │
│   ├── Manage Users              │
│   └── Statistics                │
└─────────────────────────────────┘
```

Меню **Cashback** з'являється **після** меню **WooCommerce**!
